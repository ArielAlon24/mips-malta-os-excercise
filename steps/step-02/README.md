# Step 02 - MIPS Basics

### Tasks

1. Create a simple C program that runs in an infinite loop, and run it with `qemu-mips`

2. Build the program as a static executable without linking against libc.

3. What is the current entry point address? Change it to a differnet number, without breaking your program.

### Questions

> [!NOTE]
> What is a linker script? and when should it be used?
